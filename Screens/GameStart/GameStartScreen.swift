import SwiftUI

struct GameStartScreen: View {
  var body: some View {
    VStack {
      Button {
        
      } label: {
        Text("Start")
      }
      
    }
    .navigationTitle(Text("Start Game"))
  }
}
